import XCTest
@testable import pullToReach

final class pullToReachTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(pullToReach.text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
